---
- hosts: all
#  gather_facts: yes
#  become: false
  tasks:
  - debug:
      msg:
      - "host {{ hostvars[inventory_hostname].ansible_distribution }}{{ hostvars[inventory_hostname].ansible_distribution_version }}"
